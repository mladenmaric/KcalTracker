// The router is now a Riverpod provider defined in auth_provider.dart so it
// can react to authentication state changes. Import goRouterProvider from there.
export '../providers/auth_provider.dart' show goRouterProvider;
